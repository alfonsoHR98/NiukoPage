---
interface Props {
  title: string;
}

const { title } = Astro.props;
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Consultoría fiscal y legal" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="flex flex-col bg-neutral-100 min-h-screen">
    <Header />
    <main class="f flex-grow">
      <slot />
    </main>
    <Footer />
  </body>
</html>
<style is:global>
  *,
  *:before,
  *:after {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
  }

  body {
    font-family:
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
  }

  a {
    color: var(--color-text);
    text-decoration: none;
  }
</style>
